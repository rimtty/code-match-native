# Code Match 共通プロダクト仕様

この文書はiOSとAndroidで共通に守る振る舞いを定義します。カメラ、Bluetooth、権限、永続化、画面描画は各プラットフォームのネイティブAPIで実装します。

## 照合フロー

1. 作業セッションを開始する。
2. 納品書兼現品票のQRを読み取る。
3. 現品票のCode 128を読み取る。
4. 両方から品番を抽出して照合する。
5. 一致した場合だけセッション履歴と一致件数へ記録する。
6. 次の照合では読取値を破棄するが、作業セッションは継続する。

入力の順序は固定します。QR待機中にCode 128を受け取っても進まず、Code 128待機中にQRを受け取っても進みません。不一致は履歴へ保存しません。

## 品番の抽出と比較

- 標準QRは先頭10文字がカード番号（英字4文字＋数字6文字）、11〜20文字目が10文字の品目番号です。
- Code 128は `品番@管理コード` 形式で、`@` より前を品番として使います。
- 品番は大文字化し、ハイフン・空白など英数字以外を除去して比較します。
- `@` 以降の管理コードは一致判定へ影響しません。
- 標準QRとして解析できない場合だけ、正規化したQR全文に6文字以上の正規化済み品番が含まれるかを代替判定します。
- 空の値、品番を抽出できない値、異なる品番は不一致です。

標準QRのBluetooth入力は66文字固定長と必須フィールドを検証します。Code 128のBluetooth入力は `4-2-4@管理コード` の業務形式を検証し、逆順入力を受理しません。

## データとプライバシー

照合、履歴保存、音・触覚を含む基本機能は端末内で完結します。カメラ画像、不一致の値、ユーザーアカウント、分析データを外部へ送信しません。将来この境界を変更する場合は、両プラットフォームのプライバシー宣言とストア回答を更新します。

## 共通テスト資源

機械可読な入力と期待結果は [`shared/test-fixtures/matching-cases.json`](../shared/test-fixtures/matching-cases.json)、実機・エミュレーターで表示できる画像は [`shared/test-fixtures/images`](../shared/test-fixtures/images) に置きます。SwiftとKotlinの照合テストは、同じケースを満たすことを受け入れ条件とします。
